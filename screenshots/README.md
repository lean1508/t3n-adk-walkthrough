# Screenshots

Evidence for each step of the walkthrough. Each one is a view of the real
output published in this repository, so anything shown here can be checked
against the source file rather than taken on trust.

| File | What it shows |
|---|---|
| `1-repo.png` | This repository |
| `2-quickstart.png` | First authenticated call — `Connected as: did:t3n:c0e8…` |
| `3-registrado.png` | Registration — `contract id 508` |
| `4-contrato-funcionando.png` | The contract running: `prepare` approved, `publish` still not |

The address on line 2 of `2-quickstart.png` is the **public** Ethereum address
derived from the developer key, not the key itself.
